# Mission Control — Live Agent Activity Preview

**Temporary static preview** — sandbox demo of the Live Agent Activity pipeline concept.

- Self-contained `index.html` — all CSS/JS inline, no external assets
- Animated 5-station pipeline: Incoming → CEO Dispatch → Worker Process → Ops Verify → Gateway Report
- Mock live data updates every 3 seconds

⚠️ **This is a sandbox preview only. No production data, secrets, or backend code included.**